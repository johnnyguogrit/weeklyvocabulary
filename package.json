{
  "name": "weeklyvocabulary",
  "private": true,
  "scripts": {
    "build": "cd next-app && npm run build",
    "dev": "cd next-app && npm run dev",
    "start": "cd next-app && npm run start",
    "install": "cd next-app && npm install"
  }
}
